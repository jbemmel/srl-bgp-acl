name: bgp-acl-lab

topology:
  kinds:
    srl:
      image: srl/bgp-acl:latest
      extras:
         srl-agents: [ ./bgp-acl-agent.yml ]

  nodes:
    spine1: { kind: srl, startup-config: spine1.cfg }
